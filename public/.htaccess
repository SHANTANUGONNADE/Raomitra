# Keep existing public files working; route /api/* to the PHP router when mod_rewrite is on.
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^api/(.*)$ api/index.php?r=/$1 [QSA,L]
</IfModule>
